Generate exactly one video from the description below.

Call image_to_video. If a reference image is listed, that file is the opening frame. Further reference images guide the clip.

If the request names an aspect ratio, pass it as aspect_ratio.

Print only the absolute path of the saved video file.
